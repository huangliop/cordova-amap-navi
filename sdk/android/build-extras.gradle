repositories {
    jcenter()
}
android {
    defaultConfig {
        ndk {
            abiFilters "armeabi"
        }
    }
}

dependencies {
    compile 'com.amap.api:navi-3dmap:latest.integration'
}
